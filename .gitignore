node_modules/
dist/
.DS_Store

# Kept locally only. Anchored with a leading slash so these match the folders at
# the repository root and not, say, content/blog/asoiaf.
/hugo/
/inspo/
/kanyenet/
/randomchoices/
/asoiaf/
